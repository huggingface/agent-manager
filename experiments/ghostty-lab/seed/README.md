# Seed

`workspace/CLAUDE.md` is committed. The memory files are NOT: they are the real
the-gatherer agent's working notes, and this repository is public.

Populate them locally before deploying (the lab Space is private):

    ./seed/populate.sh

Without them the lab still runs; the two Claude sessions just start with less
context, which is fine for measuring attach and resize behaviour.
